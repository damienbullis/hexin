{
    "name": "hex",
    "module": "dist/index.js",
    "types": "dist/index.d.ts",
    "type": "module",
    "scripts": {
        "build": "bun run --bun tsup",
        "lint": "oxlint && bunx --bun tsc --noEmit",
        "format:check": "bun prettier . --check",
        "format": "bun prettier . --write"
    },
    "devDependencies": {
        "@types/bun": "^1.1.14",
        "oxlint": "^0.15.3",
        "prettier": "3.4.2",
        "tsup": "^8.3.5"
    },
    "peerDependencies": {
        "typescript": "^5.7.2"
    }
}
